# Example environment variables (do NOT store secrets here in repo)
PORT=3000
NODE_ENV=production

# RDS MySQL endpoint values (replace in deployment)
DB_HOST=your-rds-endpoint.rds.amazonaws.com
DB_USER=phoneshop
DB_PASSWORD=CHANGE_ME
DB_NAME=PhoneShop

# Optional: tuning
DB_CONNECTION_LIMIT=10
ALB_HEALTH_PATH=/api/health
